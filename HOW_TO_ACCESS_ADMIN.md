# 🔐 Как попасть в панель супер-админа

## Способ 1: Через регистрацию и назначение роли (Рекомендуется)

### Шаг 1: Зарегистрируйтесь
1. Перейдите на страницу регистрации: `/auth/register`
2. Заполните форму регистрации (email и пароль)
3. После регистрации вы автоматически войдете в систему

### Шаг 2: Назначьте себе роль супер-админа

1. Откройте **Supabase Dashboard** → **SQL Editor**

2. Найдите ваш `user_id` по email:
```sql
SELECT id, email FROM auth.users WHERE email = 'ваш-email@example.com';
```

Или посмотрите всех пользователей:
```sql
SELECT id, email, created_at FROM auth.users ORDER BY created_at DESC;
```

3. Скопируйте ваш `id` (UUID) из результата запроса

4. Назначьте роль супер-админа:
```sql
UPDATE profiles
SET role = 'super_admin'
WHERE id = 'ваш-user-id-из-выше-запроса';
```

**Пример:**
```sql
UPDATE profiles
SET role = 'super_admin'
WHERE id = '123e4567-e89b-12d3-a456-426614174000';
```

### Шаг 3: Войдите в систему
1. Выйдите из системы (если вы уже вошли)
2. Перейдите на `/auth/login`
3. Введите ваш email и пароль
4. После входа система автоматически перенаправит вас на `/admin/dashboard`

---

## Способ 2: Если вы уже зарегистрированы

### Если вы уже зарегистрированы, но у вас нет роли супер-админа:

1. Откройте **Supabase Dashboard** → **SQL Editor**

2. Найдите ваш `user_id`:
```sql
SELECT id, email FROM auth.users WHERE email = 'ваш-email@example.com';
```

3. Назначьте роль:
```sql
UPDATE profiles
SET role = 'super_admin'
WHERE id = 'ваш-user-id';
```

4. Выйдите и войдите снова в систему через `/auth/login`

---

## 📍 URL админ-панели

После назначения роли `super_admin` и входа в систему, вы автоматически попадете на:

**Главная страница админ-панели:**
```
/admin/dashboard
```

**Доступные разделы админ-панели:**
- `/admin/dashboard` - Панель управления (статистика)
- `/admin/users` - Управление пользователями
- `/admin/restaurants` - Управление складами/магазинами
- `/admin/products` - Управление продуктами
- `/admin/banners` - Управление баннерами

---

## 🔍 Проверка текущей роли

Чтобы проверить вашу текущую роль, выполните в Supabase SQL Editor:

```sql
SELECT 
  p.id,
  u.email,
  p.role,
  p.is_active
FROM profiles p
JOIN auth.users u ON u.id = p.id
WHERE u.email = 'ваш-email@example.com';
```

---

## ⚠️ Важные замечания

1. **Безопасность**: Роль `super_admin` дает полный доступ ко всей системе. Будьте осторожны при назначении этой роли.

2. **Автоматический редирект**: После входа система автоматически перенаправляет пользователей в зависимости от их роли:
   - `super_admin` → `/admin/dashboard`
   - `manager` → `/manager/dashboard`
   - `collector` → `/collector/orders`
   - `courier` → `/courier/orders`
   - `customer` → `/` (главная страница)

3. **Защита маршрутов**: Все страницы админ-панели защищены и проверяют роль пользователя. Если у вас нет роли `super_admin`, вы будете перенаправлены на главную страницу.

---

## 🛠️ Альтернативный способ: Создание админа через SQL

Если вы хотите создать супер-админа напрямую через SQL (не рекомендуется для продакшена):

```sql
-- 1. Создайте пользователя в auth.users (через Supabase Auth или через SQL)
-- 2. Затем обновите профиль:
UPDATE profiles
SET role = 'super_admin'
WHERE id = 'user-id-из-auth.users';
```

---

## 📝 Быстрая команда для назначения роли

Если вы знаете email пользователя, можно выполнить одним запросом:

```sql
UPDATE profiles
SET role = 'super_admin'
WHERE id IN (
  SELECT id FROM auth.users WHERE email = 'ваш-email@example.com'
);
```

---

## ❓ Проблемы и решения

### Проблема: После входа не перенаправляет в админ-панель
**Решение:**
1. Проверьте, что роль назначена правильно (см. "Проверка текущей роли")
2. Выйдите и войдите снова
3. Очистите кеш браузера

### Проблема: "Access denied" при попытке открыть `/admin/dashboard`
**Решение:**
1. Убедитесь, что роль `super_admin` назначена в таблице `profiles`
2. Проверьте, что вы вошли в систему
3. Выйдите и войдите снова

### Проблема: Не могу найти свой user_id
**Решение:**
Выполните запрос для просмотра всех пользователей:
```sql
SELECT 
  u.id,
  u.email,
  p.role,
  u.created_at
FROM auth.users u
LEFT JOIN profiles p ON p.id = u.id
ORDER BY u.created_at DESC;
```

---

## 📚 Дополнительная информация

- Файл с SQL скриптом: `supabase/04_create_admin.sql`
- Основная документация: `README.md`
- Настройка Supabase: `SUPABASE_SETUP.md`

