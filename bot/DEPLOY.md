# 🚂 Деплой бота на Railway

## Настройка Railway

### 1. Создание проекта на Railway

1. Перейдите на [Railway](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo" или "Empty Project"

### 2. Настройка переменных окружения

В настройках проекта Railway добавьте переменную окружения:

```
TELEGRAM_BOT_TOKEN=8393509629:AAEIogSE6Z5ltFvWYt8TPDi0EtoNBMlWzio
```

### 3. Настройка деплоя

**КРИТИЧЕСКИ ВАЖНО:**

1. **Root Directory**: Должен быть `/` (корень проекта), а НЕ `/bot`!
   - Перейдите в **Settings** → **Service**
   - Найдите **Root Directory**
   - Установите значение: `/` (корень проекта) или оставьте пустым

2. **Builder**: Должен быть установлен на **DOCKERFILE**
   - Перейдите в **Settings** → **Build**
   - Убедитесь, что выбран **Dockerfile** builder
   - Если видите **Nixpacks**, измените на **Dockerfile**
   - Dockerfile Path: `Dockerfile.bot`

3. **Если Railway все еще использует Nixpacks:**
   - В настройках сервиса найдите **Build Command** и очистите его
   - Убедитесь, что файл `railway.json` существует в корне проекта
   - Перезапустите деплой вручную

### 5. Проверка деплоя

После деплоя проверьте логи в Railway Dashboard. Вы должны увидеть:
```
🤖 Baraka Telegram bot ishga tushdi!
Bot is running and waiting for messages...
```

## 🔧 Альтернативные способы деплоя

### Использование отдельного сервиса для бота

Если вы хотите запустить бота как отдельный сервис:

1. Создайте новый сервис в Railway
2. Подключите тот же GitHub репозиторий
3. Настройте:
   - **Root Directory**: `/` (или оставьте пустым)
   - **Start Command**: `npm run bot:start`
   - **Build Command**: (оставьте пустым, так как бот не требует сборки)

### Переменные окружения

Рекомендуется использовать переменные окружения для токена:

1. В Railway Dashboard → Variables
2. Добавьте: `TELEGRAM_BOT_TOKEN=ваш_токен`
3. Бот автоматически использует эту переменную

## 📝 Проверка работы бота

После деплоя:

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Проверьте, что бот отвечает и показываются инлайн кнопки

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что токен правильный
3. Проверьте, что переменная окружения `TELEGRAM_BOT_TOKEN` установлена

### Ошибки при деплое

1. Убедитесь, что все зависимости установлены (`package.json`)
2. Проверьте, что `tsx` установлен как devDependency
3. Проверьте логи сборки в Railway

### Бот падает

1. Проверьте логи на наличие ошибок
2. Убедитесь, что токен валидный
3. Проверьте интернет-соединение Railway сервера

## 🔄 Обновление бота

После изменений в коде:

1. Закоммитьте изменения в git
2. Запушьте изменения: `git push`
3. Railway автоматически задеплоит новую версию (если настроен автодеплой)
4. Бот перезапустится автоматически

### Настройка автодеплоя на Railway

Если бот не пересобирается автоматически:

1. **Проверьте настройки GitHub интеграции:**
   - В Railway Dashboard → Settings → Source
   - Убедитесь, что подключен GitHub репозиторий
   - Проверьте, что выбран правильный branch (обычно `main` или `master`)

2. **Включите автодеплой:**
   - В настройках сервиса → Settings → Deploy
   - Убедитесь, что "Auto Deploy" включен
   - Проверьте, что выбран правильный branch

3. **Проверьте webhook:**
   - Railway должен создать webhook в GitHub репозитории
   - Проверьте в GitHub: Settings → Webhooks
   - Должен быть webhook от Railway

4. **Ручной деплой:**
   - Если автодеплой не работает, можно деплоить вручную:
   - В Railway Dashboard → Deployments → "Deploy"

5. **Проверьте логи:**
   - В Railway Dashboard → Deployments
   - Проверьте последний деплой и его логи

## 📚 Дополнительная информация

- [Railway Documentation](https://docs.railway.app)
- [Telegram Bot API](https://core.telegram.org/bots/api)

