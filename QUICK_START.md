# 🚀 Быстрый старт для тестирования

## Шаг 1: Настройка Supabase (если еще не сделано)

1. Создайте проект в [Supabase](https://supabase.com)
2. Получите ключи из Settings → API
3. Выполните SQL миграции в Supabase SQL Editor:
   - `supabase/01_initial_schema.sql`
   - `supabase/02_rls_policies_safe.sql`
   - `supabase/03_enable_realtime.sql`
4. Создайте Storage buckets: `banners`, `restaurants`, `dishes`, `categories` (публичные)

Подробная инструкция: `SUPABASE_SETUP.md`

---

## Шаг 2: Создание .env.local

Создайте файл `.env.local` в корне проекта:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-ключ
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

⚠️ **Важно**: Замените значения на ваши реальные ключи из Supabase!

---

## Шаг 3: Запуск проекта

```bash
npm install  # если еще не установлены зависимости
npm run dev
```

Откройте: [http://localhost:3000](http://localhost:3000)

---

## Шаг 4: Настройка подтверждения email

**Важно**: Если при входе появляется ошибка "Email not confirmed", выполните одно из решений:

### Вариант А: Отключить подтверждение email (рекомендуется для разработки)

1. В Supabase Dashboard: **Authentication** → **Settings**
2. Найдите **"Enable email confirmations"** и **отключите** его
3. Сохраните изменения

### Вариант Б: Автоматическое подтверждение (альтернатива)

Выполните SQL из `supabase/05_auto_confirm_email.sql` в Supabase SQL Editor.
Это автоматически подтвердит всех существующих и новых пользователей.

Подробнее: `FIX_EMAIL_CONFIRMATION.md`

---

## Шаг 5: Первый вход

1. Перейдите на `/auth/register`
2. Зарегистрируйтесь
3. Откройте Supabase SQL Editor и выполните:

```sql
-- Найти ваш user_id
SELECT id, email FROM auth.users WHERE email = 'ваш-email@example.com';

-- Назначить роль супер-админа (замените на ваш user_id)
UPDATE profiles
SET role = 'super_admin'
WHERE id = 'ваш-user-id';
```

4. Выйдите и войдите снова
5. Вы будете перенаправлены на `/admin/dashboard`

---

## ✅ Готово! Можно тестировать

Теперь вы можете:
- Создавать рестораны, блюда, баннеры (как админ)
- Просматривать меню и оформлять заказы (как клиент)
- Управлять заказами (как менеджер)
- Собирать и доставлять заказы (как сборщик/курьер)

Полная инструкция по тестированию: `TESTING.md`

